<div id="breadcrumb">
  <div>Select</div>
  <div>Change RAID level?</div>
</div>
<br>
<h4>Current RAID level of <%= model.get('pool').get('name') %> is <%= model.get('pool').get('raid') %></h4>
<div class="alert alert-warning">
  <h4>
    Note! You cannot change RAID level later without adding disks at that time. If you would like to change the RAID level of <%= model.get('pool').get('name') %> along with adding disks, please select Yes below.
  </h4>
</div>
<h4>Would you like to change RAID level of this pool also?</h4>
<form class="form" id="raid-change-form">
  <div class="control-group">
    <div class="controls">
      <label class="radio inline">
        <input type="radio" name="raidChange" value="no" checked="checked">No
      </label>
      <label class="radio inline">
        <input type="radio" name="raidChange" value="yes">Yes
      </label>
    </div>
  </div>
</form>

