<table class="table table-condensed table-bordered table-hover table-striped tablesorter">
<thead>
  <tr>
    <th>Resource type</th>
    <th>Name ... Value</th>
  </tr>
</thead>

  <% for (s in share_map) { %>
    <tr>
      <td>Share</td>
      <td><%= s %> ... <%= share_map[s] %></td>
    </tr>
  <% } %>

  <% for (p in port_map) { %>
    <tr>
      <td>Port</td>
      <td><%= p %> ... <%= port_map[p] %></td>
    </tr>
  <% } %>

  <% for (c in cc_map) { %>
    <tr>
      <td>Custom</td>
      <td><%= c %> ... <%= cc_map[c] %></td>
    </tr>
  <% } %>
</table>
