<div class="row">
  <div class="col-md-8">
    <label class="control-label"></label>

    <div class="form-box">
      <form id="port-select-form" name="aform" >

	<div class="messages"></div>

	<div class="form-group">
	  <% ports.each(function(port, index) { %>
	  <label class="control-label" for="ports"><%= port.get('label') %><span class="required"> *</span></label>

	  <div class="">
	    <input type="text" id="<%= port.id %>" name="<%= port.id %>" value="<%= port.get('containerp')%>">
	     <i class="fa fa-exclamation-circle" title="<%= port.get('description') %>"></i>
	  </div>
	  <% }); %>
	</div>

      </form>
    </div>

  </div>

</div>
